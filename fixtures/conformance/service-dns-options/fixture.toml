schema = 1
id = "service-dns-options"
suite = "conformance"
description = "Isolates provider config handling of omitted, explicitly empty, ordered, and exact-duplicate raw service dns_opt strings."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider retains or specifically rejects each raw service dns_opt sequence; planned rows make no resolver-option grammar, runtime, or cross-provider support claim."

[extensions.conformance]
operation = "provider-config"
features = ["service-dns-options"]
schema = 1
reviewed = "2026-08-06"
