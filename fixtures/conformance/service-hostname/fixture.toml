schema = 1
id = "service-hostname"
suite = "conformance"
description = "Isolates provider config handling of omitted and conservatively valid service hostnames."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider retains omitted, uppercase, digit-leading, and multi-label hostname choices or returns a feature-specific rejection; planned rows make no runtime name-resolution claim."

[extensions.conformance]
operation = "provider-config"
features = ["service-hostname"]
