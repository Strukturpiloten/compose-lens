schema = 1
id = "short-bind-selinux"
suite = "conformance"
description = "Isolates provider config handling of short bind-mount SELinux relabel syntax."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider either retains the short z option or returns a feature-specific rejection."

[extensions.conformance]
operation = "provider-config"
features = ["short-bind-selinux"]
