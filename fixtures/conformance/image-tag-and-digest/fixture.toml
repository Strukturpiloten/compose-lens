schema = 1
id = "image-tag-and-digest"
suite = "conformance"
description = "Isolates provider config handling of an image reference that combines a tag and digest."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider either renders the exact tag-plus-digest image or returns a feature-specific rejection."

[extensions.conformance]
operation = "provider-config"
features = ["image-tag-and-digest"]
