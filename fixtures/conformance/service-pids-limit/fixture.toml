schema = 1
id = "service-pids-limit"
suite = "conformance"
description = "Isolates provider config handling of omitted, unlimited, finite, arbitrary-precision, zero, fractional, and arbitrary-string service PID limits."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider retains or specifically rejects each schema-shaped PID-limit spelling; planned rows make no runtime, cgroup, or cross-provider support claim."

[extensions.conformance]
operation = "provider-config"
features = ["service-pids-limit"]
