schema = 1
id = "service-init"
suite = "conformance"
description = "Isolates provider config handling of omitted and explicitly enabled or disabled service init choices."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider distinguishes omitted init from explicit true and false, or returns a feature-specific rejection."

[extensions.conformance]
operation = "provider-config"
features = ["service-init"]
