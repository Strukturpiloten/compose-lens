schema = 1
id = "service-mem-limit"
suite = "conformance"
description = "Isolates provider config handling of omitted, documented-unit, explicit-byte, leading-zero, numeric, fractional, bare-string, zero, uppercase, and IEC service memory limits."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider retains or specifically rejects each schema-shaped memory-limit spelling; planned rows make no default, runtime enforcement, normalization, host, cgroup, or cross-provider support claim."

[extensions.conformance]
operation = "provider-config"
features = ["service-mem-limit"]
