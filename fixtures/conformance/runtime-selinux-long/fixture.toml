schema = 1
id = "runtime-selinux-long"
suite = "conformance"
description = "Defines an isolated runtime probe for long bind.selinux relabel effects."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The future runtime runner must supply an already-loaded digest-pinned image and a new isolated workspace; no registry access is allowed."

[expectations]
summary = "The runtime record captures host labels before creation, after creation, and after cleanup without equating provider acceptance with a relabel effect."

[extensions.conformance]
operation = "runtime-effect"
features = ["long-bind-selinux"]
