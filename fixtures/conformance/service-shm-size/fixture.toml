schema = 1
id = "service-shm-size"
suite = "conformance"
description = "Isolates provider config handling of omitted, documented-unit, explicit-byte, leading-zero, numeric, fractional, bare-string, zero, uppercase, and IEC service shared-memory sizes."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The conformance runner clears inherited variables; the fixture contains no interpolation expressions or runtime operation."

[expectations]
summary = "The provider retains or specifically rejects each schema-shaped shared-memory-size spelling; planned rows make no default, runtime allocation, normalization, or cross-provider support claim."

[extensions.conformance]
operation = "provider-config"
features = ["service-shm-size"]
