schema = 1
id = "phase-two-field-forms"
suite = "typed-model"
description = "Protects typed service fields, independent entrypoint/command forms, omitted/literal/deferred init choices, top-level resources, alternate syntax forms, deferred expressions, extensions, and unknown fields."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "No interpolation or runtime is used; expressions remain deferred typed values."

[expectations]
summary = "Every typed field is extracted without normalization or structural diagnostics, including null, empty, scalar, and list entrypoint values plus omitted, true, false, and deferred init choices."

[extensions.typed-model]
expected_diagnostics = []
