schema = 1
id = "phase-two-field-forms"
suite = "typed-model"
description = "Protects Phase 2 typed service fields, top-level resources, alternate syntax forms, deferred expressions, extensions, and unknown fields."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "No interpolation or runtime is used; expressions remain deferred typed values."

[expectations]
summary = "Every Phase 2 field is extracted without normalization or structural diagnostics, including null and empty values."

[extensions.typed-model]
expected_diagnostics = []
