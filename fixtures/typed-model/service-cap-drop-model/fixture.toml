schema = 1
id = "service-cap-drop-model"
suite = "typed-model"
description = "Protects source-aware service cap_drop omission, explicit emptiness, exact strings, uniqueItems diagnostics, and malformed-item recovery."
secrets_reviewed = true
files = ["compose.yaml"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "No interpolation environment is supplied; the dollar-bearing capability string remains deferred and non-sensitive."

[expectations]
summary = "Only YAML string items are typed; exact duplicates remain inspectable with diagnostics, case variants remain distinct, malformed items do not erase valid siblings, and omission differs from an explicit empty sequence."
