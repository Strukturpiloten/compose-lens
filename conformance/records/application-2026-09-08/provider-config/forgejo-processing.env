# Public, test-only values for config rendering. Referenced paths need not exist.
BF_FORGEJO_CONFIG_ROOT=${CAPTURE_ROOT}/nonexistent/binds/forgejo
FROM_CALLER=from-explicit-env-file
BF_FORGEJO_DB_PASSWORD=public-test-only-db-canary
